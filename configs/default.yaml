# @package _global_

defaults:
  - /utilities_strategy/dc
  - _self_

reward_type: tw
learning_algorithm: oracle

# Type of the kernel used for Bayesian optimization (linear, exponential or matern)
kernel_type: linear

RKHS_bound: 0.2

horizon: 30
epsilon: ${div:1,${horizon}}
delta: 0.02

learn_utilities: False